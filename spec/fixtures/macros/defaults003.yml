- :deploy:
    - :defaults:
        a: a
        b: b
        c: c
    - a: "{a}"
    - b: "{b}"
    - c: "{c}"

- job:
    name: "defaults"
    list:
      .deploy:
        a: A
        b: B
        c: C
